# Seed data

The initial migration seeds source and category reference data only. Dashboard demo data lives in `apps/web/src/data/trends.ts` and is intentionally labeled as fixture data until a Supabase project is connected.
